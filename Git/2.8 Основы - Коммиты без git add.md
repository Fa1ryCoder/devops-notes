# 2.8 Git – Основы – Коммиты без git add

## Содержание

1. [Флаг -a — автоматическое индексирование](#1-флаг--a--автоматическое-индексирование)
2. [Коммит конкретных файлов по пути](#2-коммит-конкретных-файлов-по-пути)
3. [Алиас для коммита всех изменений](#3-алиас-для-коммита-всех-изменений)

---

В предыдущих темах мы использовали двухступенчатый процесс: `git add` → `git commit`. Но в Git есть способы ускорить этот процесс и обойтись без явного `git add`.

---

## 1. Флаг -a — автоматическое индексирование

Флаг `-a` (`--all`) заставляет Git автоматически добавить все изменённые отслеживаемые файлы в индекс и сразу создать коммит — объединяет `git add` и `git commit` в одну операцию:

```bash
git commit -a -m "fix: исправить опечатку в конфиге"

# Или сокращённо
git commit -am "fix: исправить опечатку в конфиге"
```

**Важное ограничение:** флаг `-a` игнорирует новые (`untracked`) файлы которые ещё не были добавлены в систему контроля версий. Если создал новый файл — этот способ для него не сработает:

```bash
touch new-file.py
git commit -am "add new file"
# new-file.py НЕ попадёт в коммит — он untracked
```

**Ещё один нюанс:** `-a` подхватывает вообще все изменённые отслеживаемые файлы — в том числе те, что уже были застейджены вручную через `git add` до этого. Флаг не разбирает что именно ты хотел закоммитить — в коммит уйдёт всё сразу:

```bash
git add file1.py                    # застейджил вручную, планировал коммитить только его
echo "правка" >> file2.py           # заодно чуть поправил другой файл, add не делал

git commit -am "fix: обновить file1"
# в коммит попадут ОБА файла — и file1.py, и file2.py, а не только застейдженный
```

**Когда использовать:** удобно когда вносишь правки только в уже существующие файлы и уверен что все изменения формируют один логичный коммит.

---

## 2. Коммит конкретных файлов по пути

Можно указать Git какие именно файлы нужно закоммитить прямо сейчас — даже если в рабочей директории есть другие изменения:

```bash
git commit auth.py -m "fix: исправить валидацию токена"
# Только auth.py попадёт в коммит
# Остальные изменения в проекте остаются нетронутыми
```

**Под капотом:** да, стейджинг всё равно происходит — просто автоматически и только для указанного файла. Причём берётся не то, что было застейджено раньше через `git add`, а актуальное содержимое рабочей директории на момент коммита. Если файл уже был частично застейджен, эта промежуточная версия игнорируется:

```bash
echo "v1" > auth.py
git add auth.py              # застейджили v1

echo "v2" >> auth.py         # доработали дальше, БЕЗ повторного git add

git commit auth.py -m "fix: доработка"
# В коммит уйдёт v1+v2 — актуальное содержимое рабочей директории,
# а не только застейдженная ранее v1
```

После коммита индекс для этого файла синхронизируется с тем, что попало в коммит — как будто ты сначала явно сделал `git add`, а потом `git commit`.

Можно указать сразу несколько файлов через пробел:

```bash
git commit auth.py utils.py -m "fix: исправить валидацию и утилиты"
# В коммит попадут изменения только этих двух файлов, остальное не тронется
```

**Ограничение:** как и флаг `-a`, этот метод работает только для уже отслеживаемых файлов. Попытка закоммитить новый файл таким способом завершится ошибкой:

```bash
touch new-file.py
git commit new-file.py -m "add file"
# error: pathspec 'new-file.py' did not match any file(s) known to git
```

**Когда использовать:** когда в проекте ведётся параллельная работа над несколькими задачами и нужно срочно зафиксировать одно конкретное исправление не затрагивая остальной код.

По сути это альтернатива `git commit -am`, только с другим охватом:

```bash
# git commit -am — все отслеживаемые файлы
git commit -am "fix: исправления"

# git commit <файл> — только конкретный файл, остальные изменения не трогает
git commit auth.py -m "fix: исправить валидацию"
```

Причина одна и та же для обоих ограничений: это сделано специально для безопасности — чтобы разработчик случайно не закоммитил лишнее (временные файлы, результаты сборки, логи). Git требует явного `git add`, чтобы подтвердить, что новый файл действительно нужно отслеживать.

---

## 3. Алиас для коммита всех изменений

В Git нет встроенного флага который бы добавлял и новые файлы и изменения в старых за один шаг. Но это можно реализовать через алиас:

```bash
# Алиас который сначала делает git add -A, потом git commit
git config --global alias.ca "!git add -A && git commit"

# Использование
git ca -m "feat: добавить авторизацию"
```

Технически `git add` тут никуда не девается — алиас всё равно вызывает его первым шагом. Просто вводить его руками отдельной командой больше не нужно: `git ca` делает оба шага сразу.

**Разница между `git add .` и `git add -A` в алиасе:**
- `git add .` — добавит изменения только из текущей директории и вложенных
- `git add -A` — добавит все изменения во всём проекте независимо от того в какой папке находишься

> **Осторожно:** не злоупотребляй такими алиасами, сваливая все изменения в один большой коммит с невнятным описанием. Используй только если уверен что все изменения логически формируют один хороший коммит.

---

*Предыдущая тема: [2.7 Git – Основы – Зачем нужен индекс?](./2.7%20Основы%20-%20Зачем%20нужен%20индекс.md)*
*Следующая тема: [2.9 Git – Основы – Удаление и переименование файлов](./2.9%20Основы%20-%20Удаление%20и%20переименование%20файлов.md)*
